namespace ClothingStoreManager.Helpers
{
    public static class ExportHelper
    {
        // تصدير إلى PDF (يحتاج مكتبة iTextSharp)
        public static void ExportToPdf(string filePath, string content)
        {
            // هنا تضاف أكواد التصدير باستخدام iTextSharp
        }

        // تصدير إلى Excel (يحتاج مكتبة EPPlus)
        public static void ExportToExcel(string filePath, object data)
        {
            // هنا تضاف أكواد التصدير باستخدام EPPlus
        }
    }
}